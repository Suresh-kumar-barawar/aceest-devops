apiVersion: v1
kind: Service
metadata:
  name: aceest-service
  namespace: aceest-devops
spec:
  selector:
    app: aceest-app
    track: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort

apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-blue
  namespace: aceest-devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aceest-app
      color: blue
  template:
    metadata:
      labels:
        app: aceest-app
        color: blue
    spec:
      containers:
        - name: aceest-app
          image: aceest-fitness-gym:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: APP_VERSION
              value: blue
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: aceest-config
                  key: PORT

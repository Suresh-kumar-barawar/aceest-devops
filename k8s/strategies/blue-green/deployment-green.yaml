apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-green
  namespace: aceest-devops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aceest-app
      color: green
  template:
    metadata:
      labels:
        app: aceest-app
        color: green
    spec:
      containers:
        - name: aceest-app
          image: aceest-fitness-gym:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: APP_VERSION
              value: green
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: aceest-config
                  key: PORT

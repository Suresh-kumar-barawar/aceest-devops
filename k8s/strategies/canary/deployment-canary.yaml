apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-canary
  namespace: aceest-devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aceest-app
      release: canary
  template:
    metadata:
      labels:
        app: aceest-app
        release: canary
    spec:
      containers:
        - name: aceest-app
          image: aceest-fitness-gym:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: APP_VERSION
              value: canary
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: aceest-config
                  key: PORT

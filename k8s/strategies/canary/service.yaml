apiVersion: v1
kind: Service
metadata:
  name: aceest-canary-service
  namespace: aceest-devops
spec:
  selector:
    app: aceest-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30083
  type: NodePort
